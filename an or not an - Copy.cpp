#include<stdio.h>
int main()
{
	char choice;
	printf(":::::ENTER WORD TO CHECK AN OR NOT AN:::::\n");
	scanf("%c",&choice);
	switch(choice)
	{
			case 'a':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'e':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'i':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'o':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'u':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'A':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'E':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'I':
			printf("::an  BEFORE WORD USE ::");
			break;
			case 'O':
			printf("::an BEFORE WORD USE ::");
			break;
			case 'U':
			printf("::an BEFORE WORD USE ::");
			break;
			default:
				printf("::::CONSONENT(NOT an USE **PLESE USE THIS a**)::::");
	}
	return 0;
}
