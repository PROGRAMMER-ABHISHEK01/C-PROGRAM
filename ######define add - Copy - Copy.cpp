#include<stdio.h>
#define a printf("enter two number: ")
        int main()
        {
        	int c,b;
        	a;
        	scanf("%d%d",&c,&b);
        	printf("%d",c+b);
        	return 0;
		}
        
